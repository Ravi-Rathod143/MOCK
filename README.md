# Mock E-Com Cart — Fullstack Assignment

This repo contains a mock e-commerce cart app with frontend (React) and backend (Node/Express).

## Start backend
cd backend
npm install
npm start

## Start frontend
cd frontend
npm install
npm start

Backend runs on http://localhost:5000 and frontend on http://localhost:3000
